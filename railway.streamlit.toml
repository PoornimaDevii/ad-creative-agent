[build]
builder = "nixpacks"

[deploy]
startCommand = "bash start.sh"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3

# Set these in the Railway dashboard under service variables:
# GEMINI_API_KEY = "..."
# MCP_SERVER_URL = "https://your-mcp-service.up.railway.app/sse"
